{
  "name": "financial-tracker-emulator-tests",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end Firestore tests run against the local Firebase emulator using the official @firebase/rules-unit-testing SDK. No mocks; real Firestore protocol.",
  "type": "module",
  "scripts": {
    "test": "mocha --timeout 30000 'rules/**/*.test.js'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "chai": "^6.2.0",
    "firebase": "^13.0.0",
    "mocha": "^11.7.4"
  }
}
